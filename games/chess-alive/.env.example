# OpenRouter API Key (required for LLM modes)
OPENROUTER_API_KEY=your-openrouter-api-key-here

# LLM Model to use (optional)
# Default: mistralai/devstral-2512:free (free tier)
# Other options:
#   - openai/gpt-4o-mini (paid, better quality)
#   - anthropic/claude-3-haiku (paid, fast)
#   - qwen/qwen3-coder:free (free tier)
CHESS_LLM_MODEL=mistralai/devstral-2512:free

# Stockfish path (optional, will auto-detect if installed)
STOCKFISH_PATH=/usr/bin/stockfish
